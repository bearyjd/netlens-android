source "https://rubygems.org"

# fastlane drives Play Store publishing (see fastlane/Fastfile). Run
# `bundle install` then `bundle exec fastlane <lane>`. Commit the generated
# Gemfile.lock to pin the resolved version for reproducible CI runs.
gem "fastlane"

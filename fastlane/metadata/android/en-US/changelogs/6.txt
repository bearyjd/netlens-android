NetLens has moved to a new package name (com.ventouxlabs.netlens). Because the app identity changed, this arrives as a separate app rather than an in-place update — you may need to install it fresh and remove the old NetLens. Under the hood: repaired and re-enabled unit tests across the IP Info, Security Posture, and Traceroute modules, and CI now runs the full library-module test suite on every change.

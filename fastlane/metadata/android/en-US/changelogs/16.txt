New in 1.3.2:

- One-tap location for a LAN scan — record where a scan was taken without typing coordinates. Tap "Use my location" and it fills in; if location is unavailable, the manual fields are still there. No Google Play Services required.
- Fixed LAN Scan's "Inventory" tab label wrapping mid-word on narrow screens, such as a foldable's cover display.
